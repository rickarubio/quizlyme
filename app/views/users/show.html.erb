Hello <%= @user.username %>

<div class="row">
  <div class="user-actions">
    <div class="panel panel-info">
      <div class="panel-heading">
        <h3 class="panel-title">Would you like to:</h3>
      </div>
      <div class="panel-body">
        <ul class="list-group">
          <li class="list-group-item"><%= link_to "Create a New Quiz", new_quiz_path %></li>
          <li class="list-group-item">View All Quizzes</li>
          <li class="list-group-item">Edit Quizzes</li>
        </ul>
      </div>
    </div>
  </div>
</div>

Here are Your current Quizzes:
<% if @quizzes.empty? %>
  <p>Seems you currently do not have any quizzes you can take.
<% else %>
  <% @quizzes.each do |quiz| %>
    <p><%= @quiz.name %></p>
  <% end %>
<% end %>